import java.util.HashMap;

public class symetricPairs {
    
    HashMap<Integer,Integer> hm = new Hash
    for(int i = 0; i < arr.length; i++){
        int first = arr [i] [0];
        int sec = arr [i] [1];
        Integer val = hm.get(see);
        if (val != null && val == first){
            System.out.println(see + " " + first);
        }else
            hm.put(first, see);
    }

}
